once copied:
1. Run: 
```shell
pip install -r requirements.txt
```
2. Change postgresql settings in "settings.py" inside the 
```shell 
backend/
```
folder
3. cd to backend folder and run 
```shell
python manage.py makemigrations
```
and
```shell 
python manage.py migrate
```