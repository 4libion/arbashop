once copied:
1. Run: ```console 
pip install -r requirements.txt
```
2. Change postgresql settings in "settings.py" inside the ```console backend/``` folder
3. cd to backend folder and run ```console
python manage.py makemigrations``` and ```console 
python manage.py migrate```