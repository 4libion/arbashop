once copied:
1. Run: ```bash 
pip install -r requirements.txt
```
2. Change postgresql settings in "settings.py" inside the ```backend/``` folder
3. cd to backend folder and run ```bash
python manage.py makemigrations``` and ```bash 
python manage.py migrate```